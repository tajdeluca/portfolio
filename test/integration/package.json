{
  "name": "portfolio-integration-tests",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test --trace on"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
